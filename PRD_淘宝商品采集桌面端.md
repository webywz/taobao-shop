# 淘宝商品采集桌面端 - 产品需求文档（PRD）

## 1. 文档概述

### 1.1 目的与范围

本文档定义淘宝商品采集桌面端产品的功能需求、非功能需求、MVP 范围与验收标准，面向产品、设计、开发、测试团队。

### 1.2 术语定义

- **商品采集**：通过真实浏览器会话，从淘宝商品页提取标题、价格、SKU、详情等结构化数据
- **登录态**：用户在浏览器中完成登录后的会话状态，包括 cookie 与本地存储
- **Profile**：独立的浏览器会话配置，对应一个淘宝账号的登录态
- **Sidecar**：由主应用启动的独立后台进程，负责浏览器自动化与数据提取

### 1.3 文档版本记录

| 版本 | 日期 | 作者 | 变更说明 |
|------|------|------|----------|
| 1.0 | 2026-04-24 | - | 初始版本 |

---

## 2. 产品背景

### 2.1 业务背景与痛点

电商运营、数据分析、选品人员需要批量采集淘宝商品信息用于：
- 竞品分析与价格监控
- 选品决策与市场调研
- 商品数据归档与对比

现有痛点：
- 手动复制粘贴效率低，易出错
- 浏览器插件受限于页面权限，无法深度提取
- 在线服务存在数据隐私风险，且易受平台风控限制
- 纯 HTTP 爬虫无法应对淘宝强前端渲染与登录验证

### 2.2 目标用户画像

- **电商运营**：需要定期采集竞品价格、销量、评价数据
- **数据分析师**：需要批量采集商品数据用于市场分析
- **选品人员**：需要快速浏览并保存候选商品信息

用户特征：
- 熟悉淘宝平台操作
- 对数据隐私敏感，倾向本地工具
- 可接受手动登录，但希望后续采集自动化
- 需要导出为 Excel / CSV 用于后续分析

### 2.3 产品定位

本地化、低频、可诊断的淘宝商品采集桌面工具，以用户主动触发、真实浏览器会话为核心，不追求高并发批量抓取，强调数据完整性与失败可恢复性。

---

## 3. 产品目标

### 3.1 核心目标

- 用户可在本地桌面端输入淘宝商品链接，一键采集商品基础信息
- 支持用户手动完成登录与验证，后续采集复用登录态
- 采集结果可导出为 JSON / CSV / Excel，包含标题、价格、SKU、详情、图片
- 采集失败时提供明确错误提示与调试快照，支持重试

### 3.2 成功指标（KPI）

- 单商品采集成功率 ≥ 85%（排除风控与页面结构变化）
- 登录态有效期内无需重复登录
- 采集失败时 100% 提供调试快照（截图 + HTML + 日志）
- 用户从输入链接到获得导出文件，平均耗时 ≤ 30 秒

---

## 4. 用户故事

### 4.1 登录与账号管理

**故事 1**：首次使用时添加账号
- 作为新用户，我希望首次启动时能引导我添加淘宝账号
- 我希望在受控浏览器窗口中手动完成登录与验证码
- 登录成功后，我希望后续采集无需重复登录

**故事 2**：管理多个账号
- 作为运营人员，我可能需要用不同账号采集不同店铺的数据
- 我希望能在应用中添加、切换、删除多个账号
- 切换账号时，我希望采集任务自动使用对应账号的登录态

### 4.2 商品采集

**故事 3**：采集单个商品
- 作为用户，我输入一个淘宝商品链接
- 我希望看到采集进度（打开页面 → 提取数据 → 完成）
- 采集完成后，我希望看到商品标题、价格、主图预览
- 我希望能导出为 Excel 或 JSON

**故事 4**：采集失败时的处理
- 当采集失败时，我希望看到明确的错误原因（登录过期 / 页面超时 / 风控拦截）
- 我希望能查看失败时的页面截图与日志
- 我希望能一键重试

### 4.3 数据导出

**故事 5**：导出采集结果
- 采集完成后，我希望能选择导出格式（JSON / CSV / Excel）
- 我希望导出文件包含商品图片资源（可选）
- 我希望能自定义导出路径

### 4.4 多账号切换

**故事 6**：切换账号采集
- 我希望在采集前选择使用哪个账号
- 切换账号后，我希望能立即开始采集，无需等待

---

## 5. 功能需求

### 5.1 登录态管理

**F1.1 手动登录**
- 用户点击"添加账号"，应用打开受控浏览器窗口
- 用户在浏览器中手动完成淘宝登录与验证
- 应用检测到登录成功后，保存登录态到本地 profile

**F1.2 登录态持久化**
- 登录态保存到本地独立目录，包含 cookie 与本地存储
- 后续采集任务复用已有登录态，无需重复登录
- 登录态过期时，提示用户重新登录

**F1.3 登录状态检测**
- 应用启动时检测已有账号的登录状态
- 登录过期时，在账号列表中标记状态

### 5.2 商品链接输入与批量管理

**F2.1 单链接输入**
- 用户在主界面输入淘宝商品链接
- 支持短链、带参数链接的自动规范化

**F2.2 任务列表**
- 展示历史采集任务列表
- 每个任务显示：商品标题、状态、采集时间
- 支持查看任务详情、重新采集、删除任务

### 5.3 采集任务执行与进度反馈

**F3.1 采集流程**
- 用户提交采集任务后，应用依次执行：
  1. 打开商品页
  2. 等待页面加载完成
  3. 提取 DOM、网络响应、页面状态
  4. 归一化数据
  5. 保存结果与调试快照

**F3.2 进度展示**
- 实时展示当前采集阶段（加载页面 / 提取数据 / 完成）
- 展示页面截图预览

**F3.3 错误处理**
- 采集失败时，展示错误类型与原因
- 提供调试快照下载入口（截图 + HTML + 日志）
- 提供重试按钮

### 5.4 数据预览与导出

**F4.1 结果预览**
- 采集完成后，展示商品标题、价格、主图、SKU、属性
- 支持查看完整 JSON 数据

**F4.2 导出格式**
- 支持导出为 JSON / CSV / Excel
- 可选是否下载商品图片资源到本地

**F4.3 导出路径**
- 用户可自定义默认导出目录
- 导出时可临时修改路径

### 5.5 多账号管理

**F5.1 账号列表**
- 展示已添加的账号列表
- 每个账号显示：别名、登录状态、最近使用时间

**F5.2 账号操作**
- 新建账号：引导用户登录并保存 profile
- 重命名账号：修改账号别名
- 重新登录：清除旧登录态，重新打开登录窗口
- 删除账号：删除 profile 与相关数据

**F5.3 账号切换**
- 采集任务提交时，可选择使用哪个账号
- 默认使用最近使用的账号

### 5.6 首次启动引导

**F6.1 欢迎页**
- 首次启动时展示欢迎页，说明产品功能与使用流程

**F6.2 环境检测**
- 检测浏览器依赖是否完整
- 检测磁盘空间是否充足
- 检测失败时给出修复建议

**F6.3 账号添加引导**
- 引导用户添加第一个账号
- 可跳过，后续在设置中添加

### 5.7 自动更新

**F7.1 更新检测**
- 应用启动时静默检查更新
- 发现新版本时通知用户

**F7.2 更新下载与安装**
- 用户确认后下载更新包
- 下载完成后提示重启安装

### 5.8 日志与配置管理

**F8.1 日志查看**
- 设置页提供日志查看入口
- 展示应用日志与采集任务日志

**F8.2 配置项**
- 主题切换（浅色 / 深色 / 跟随系统）
- 默认导出路径
- 采集超时时间
- 是否默认下载图片资源

---

## 6. 非功能需求

### 6.1 性能要求

- 单商品采集耗时 ≤ 30 秒（正常网络环境）
- 应用启动时间 ≤ 3 秒
- 界面操作响应时间 ≤ 200ms

### 6.2 稳定性与错误处理

- 采集失败时不崩溃，提供明确错误提示
- 浏览器进程异常退出时，主应用不受影响
- 所有采集任务保留调试快照，便于问题排查

### 6.3 安全性

- 登录态存储到本地加密目录，不上传云端
- 调试快照中的敏感信息（cookie、token）自动脱敏
- 应用内通信使用本地 IPC，不暴露网络端口

### 6.4 兼容性

- 支持 macOS 10.15+
- 支持 Windows 10+
- 支持 1920x1080 及以上分辨率

### 6.5 可用性与交互体验

- 界面简洁，核心操作不超过 3 步
- 错误提示清晰，避免技术术语
- 首次使用有引导流程

---

## 7. MVP 范围

### 7.1 MVP 包含功能

- 单账号登录与登录态持久化
- 单商品链接采集（标题、价格、主图、SKU、属性、详情）
- 采集结果导出为 JSON
- 采集失败时提供调试快照
- 任务列表与重试功能
- 基础配置项（导出路径、超时时间）

### 7.2 MVP 排除功能

- 批量并发采集
- 评论与问大家采集
- 多账号管理（首版只支持单账号）
- CSV / Excel 导出（首版只支持 JSON）
- 自动更新（首版手动下载安装）

### 7.3 后续迭代规划

- V1.1：多账号管理、CSV / Excel 导出
- V1.2：批量采集队列、自动更新
- V2.0：评论与问大家采集、店铺信息采集

---

## 8. 验收标准

### 8.1 核心流程验收用例

**用例 1：首次登录并采集**
1. 启动应用，进入欢迎页
2. 点击"添加账号"，打开浏览器窗口
3. 手动完成淘宝登录
4. 登录成功后，返回主界面
5. 输入商品链接，点击"开始采集"
6. 等待采集完成，查看商品信息预览
7. 点击"导出 JSON"，保存到本地
8. 验证导出文件包含完整商品信息

**用例 2：复用登录态采集**
1. 应用已有登录态
2. 输入新的商品链接，点击"开始采集"
3. 验证无需重新登录，直接开始采集
4. 采集完成后导出数据

**用例 3：采集失败重试**
1. 输入一个需要登录的商品链接
2. 登录态已过期
3. 采集失败，提示"登录已过期"
4. 点击"重新登录"，完成登录
5. 点击"重试"，采集成功

### 8.2 异常场景验收用例

**用例 4：网络超时**
1. 断开网络连接
2. 提交采集任务
3. 验证超时后提示"页面加载超时"
4. 验证提供调试快照下载入口

**用例 5：风控拦截**
1. 输入触发风控的商品链接
2. 采集失败，提示"风控拦截"
3. 验证提供页面截图，展示验证码页面

**用例 6：浏览器进程崩溃**
1. 采集过程中手动结束浏览器进程
2. 验证主应用不崩溃
3. 验证任务标记为失败，提示"浏览器异常退出"
