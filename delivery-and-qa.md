# 交付、研发顺序与测试

## 1. 文档目标

本文档整合开发顺序、里程碑、测试清单、上线检查和观测指标。

## 2. 推荐研发顺序

1. 卡密激活与插件设备绑定
2. 任务创建与状态查询
3. 插件拉任务、认领、心跳
4. 淘宝 extractor
5. 拼多多 extractor
6. 上传与结果提交
7. 任务详情页与图片格式转换
8. ZIP 归档
9. 历史任务页

## 3. 里程碑

### 3.1 里程碑一：任务闭环

交付：

- 卡密激活
- 插件设备绑定
- 创建任务
- 假数据状态流转

退出条件：

- 任务能从 `pending` 流转到 `completed` 或 `failed`
- Web 能看到状态变化

### 3.2 里程碑二：真实提取

交付：

- 淘宝主图 extractor
- 拼多多主图 extractor
- 失败日志和错误码

退出条件：

- 两个平台都能稳定提取主图

### 3.3 里程碑三：完整结果

交付：

- SKU 图和详情图分类
- 上传入库
- 结果详情页

退出条件：

- 主图、SKU 图、详情图可分组展示

### 3.4 里程碑四：归档与上线准备

交付：

- ZIP 归档
- 历史页
- 图片格式转换
- 监控和回归样本

退出条件：

- 成功任务能下载 ZIP
- 关键错误码可观测

## 4. 模块交付清单

### 4.1 Web

- 创建任务页
- 历史页
- 详情页
- 帮助页

### 4.2 Backend

- 卡密兑换
- 许可校验
- 设备绑定
- 任务状态机
- 上传签名
- 结果入库
- 归档 Worker
- 图片格式转换

### 4.3 Extension

- 设备注册
- 许可绑定
- 空队列退避轮询
- 标签页控制
- 淘宝 extractor
- 拼多多 extractor
- 上传与结果回传

### 4.4 基础设施

- PostgreSQL 初始化脚本
- Redis / 队列配置
- 对象存储桶和权限
- 日志与告警配置
- Staging 环境联调域名

## 5. 测试清单

### 5.1 卡密激活

- 有效卡密可成功兑换
- 已使用卡密不能再次兑换
- 激活后可获得 `license_token`
- 未激活时不能创建任务

### 5.2 设备

- 插件可注册设备
- 未绑定许可的设备不能执行任务

### 5.3 任务状态机

- 新任务进入 `pending`
- 认领后进入 `claimed`
- 执行后进入 `running`
- 上传阶段进入 `uploading`
- 完成后进入 `completed`
- 无心跳任务可回收

### 5.4 提取链路

- 淘宝普通商品主图成功
- 淘宝多规格商品主图成功
- 拼多多普通商品主图成功
- 拼多多多规格商品主图成功
- SKU 图分类正确
- 详情图分类正确

### 5.5 上传与结果

- 可获取上传签名
- 上传后可确认完成
- manifest 可提交
- 重复提交不重复入库

### 5.6 归档

- 成功任务可生成 ZIP
- ZIP 结构符合规范
- 下载链接会过期

### 5.7 图片访问与格式转换

- 激活后图片和 ZIP 可直接访问
- 单张图片可成功转换格式
- 分组批量转换可返回正确结果
- 转换失败时返回明确错误

### 5.8 历史与列表

- 历史任务可按状态筛选
- 已完成任务可从列表直接进入详情
- 分页行为稳定，不重复不漏项

### 5.9 安全与权限

- 未携带有效 `license_token` 不能访问任务结果
- 未绑定许可的设备无法认领任务
- 非当前任务的上传签名不能复用到其他任务
- 敏感日志不输出完整卡密、token、签名 URL

## 6. 上线前检查

- 卡密激活流程清晰
- 插件安装和设备绑定流程清晰
- 首页 URL 校验正常
- 详情页能显示状态、图片、下载入口
- 详情页能显示格式转换入口
- 任务状态机限制非法流转
- 归档走异步队列
- 下载链接使用签名 URL
- 敏感日志不输出明文 token
- API 错误率、成功率、ZIP 队列积压可观测

补充检查：

- Staging 完整跑通淘宝和拼多多样本
- 插件版本号与后端兼容矩阵明确
- 对象存储生命周期规则已配置
- 失败任务能在日志中按 `taskId` 快速追踪
- 关键告警已接入通知渠道

## 7. 首周观察指标

- 每日任务量
- 设备在线率
- 淘宝成功率
- 拼多多成功率
- `PAGE_TIMEOUT` 占比
- `AUTH_REQUIRED` 占比
- ZIP 平均生成时长
- 卡密兑换成功率
- 任务创建失败率
- 设备离线率
- 图片格式转换平均时长

## 8. 研发节奏建议

如果按两周一个迭代，建议：

- 第 1 迭代：卡密、设备、任务状态机、假数据联调
- 第 2 迭代：淘宝 / 拼多多主图提取、上传回传
- 第 3 迭代：SKU 图、详情图、详情页、历史页
- 第 4 迭代：ZIP、格式转换、监控、回归与上线准备

## 9. 联调顺序建议

建议跨端按这条链路联调：

1. Web 激活页 + 后端兑换接口
2. 插件注册 / 绑定 + 后端设备接口
3. Web 创建任务 + 后端任务详情接口
4. 插件拉取任务、认领、心跳
5. 单平台 extractor + 上传签名
6. 结果提交 + 详情页展示
7. ZIP 和格式转换

## 10. 验收标准

验收时至少满足：

- 两个平台各有一组稳定样本可完整跑通
- `completed` 任务的图片分组正确率达到可接受水平
- 失败任务都有明确错误码
- ZIP 下载和图片格式转换都能从详情页闭环使用
- 用户首次使用可在帮助页独立完成安装和激活

## 11. 上线与回滚预案

上线建议：

- 先灰度给内部账号或少量卡密批次
- 插件和后端版本同步发布，避免协议漂移
- 首周保留研发值守，优先处理错误码新增和平台页面变更

回滚建议：

- 后端保留上一版本可快速回切
- 插件如无法即时回滚，后端需兼容上一个插件协议版本
- 若某平台 extractor 大面积失败，可临时按平台下线入口或提示维护中

## 12. 当前延期项

- 批量任务
- 团队权限
- 支付接入
- 开放 API
- Webhook
- 多浏览器支持

## 13. 开发前冻结项

正式开工前，至少需要冻结以下决定：

- 卡密有效期规则，按时间授权，不按额度
- 一个 `license` 允许绑定几个设备
- 首页创建任务时是否允许同一用户并发发起多个任务
- `task`、`asset`、`archive` 的 ID 生成规则
- ZIP 命名规则和目录结构
- ZIP 和转换产物保留期是否固定开放为 `3天 / 7天 / 30天`
- 插件与后端的最低兼容版本策略
- 错误码和前端提示文案的映射关系

## 14. 开发任务拆解建议

建议按三个并行面拆：

- Web：激活页、创建页、详情页、历史页、帮助页
- Backend：激活与鉴权、任务与状态机、上传与结果、归档与转换
- Extension：注册绑定、轮询认领、页面提取、上传回传

推荐第一周拆解：

- Backend 先给出激活、设备、任务创建、任务详情 4 个核心接口
- Web 先跑通激活页和任务详情假数据状态
- Extension 先完成注册、绑定、空轮询，不急着先做完整 extractor

## 15. 开发就绪判断

满足以下条件再进入正式开发最稳：

- API 字段级协议已冻结
- 数据库表结构和索引方案已冻结
- 插件和 Web 的消息协议已冻结
- 至少各准备 3 个淘宝样本和 3 个拼多多样本
- 错误码清单已可用于前端直接接入

如果缺其中任一项，也能开工，但会显著增加联调返工概率

## 16. 优先级测试建议

建议把测试拆成三个优先级：

- `P0`
  - 卡密激活成功
  - 插件绑定成功
  - 单链接任务创建成功
  - 至少一个平台主图提取成功
  - 详情页能显示结果
- `P1`
  - 淘宝 / 拼多多双平台都可跑通
  - SKU 图和详情图分类正确
  - ZIP 可下载
  - 格式转换可用
- `P2`
  - 历史页筛选和搜索
  - 边界错误码展示
  - 对象存储过期和清理策略验证

## 17. 第一阶段验收口径

如果只追求首个可演示版本，建议验收口径收敛为：

- Chrome 插件可安装并绑定
- 用户激活后能提交 1 条淘宝或拼多多商品链接
- 插件能本地完成提取并回传至少主图
- 详情页能展示主图和基础状态
- 失败时能展示稳定错误码

有了这 5 点，就可以进入第二阶段再补 SKU 图、详情图、ZIP 和格式转换。

## 18. 当前建议默认口径

基于当前已确认信息，建议直接按以下口径开工：

- 卡密采用时间授权模型，不做任务额度
- 一个 `license` 首版只绑定一个设备
- ZIP 与转换产物保留期统一支持 `3天 / 7天 / 30天`
- 同一 `license` 下允许保留历史任务，但运行态任务默认串行
- 第一阶段只以主图闭环验收，第二阶段再补全 SKU 图、详情图、ZIP 和转换

## 19. 开发启动清单

正式开工前，建议先完成这 10 项准备：

1. 冻结接口字段和错误码，不再边写边改名字
2. 冻结数据库表结构和主索引
3. 确认一个 `license` 只绑定一个设备
4. 确认 ZIP 与转换产物保留期只开放 `3天 / 7天 / 30天`
5. 准备至少 3 条淘宝样本和 3 条拼多多样本
6. 建好 Staging 环境的 API、对象存储和 Redis
7. 建好 Chrome 插件本地联调环境
8. 确认第一阶段只验收到“主图闭环”
9. 明确后端兼容“当前插件版本和上一个插件版本”
10. 明确谁负责样本维护和回归验证

## 20. 首周派工建议

建议按三个开发角色并行：

### 20.1 后端

首周目标：

- 完成 `licenses`、`devices`、`extract_tasks`、`task_assets` 四张核心表
- 完成卡密兑换、当前许可查询、设备注册、设备绑定接口
- 完成任务创建、任务详情、任务列表接口
- 完成任务拉取、认领、心跳、失败回传接口

首周交付标准：

- Web 能拿真实接口完成激活
- 插件能完成注册和绑定
- 能创建任务并看到状态变化

### 20.2 前端

首周目标：

- 完成激活页
- 完成首页创建任务页
- 完成详情页基础状态展示
- 完成插件安装 / 绑定状态展示

首周交付标准：

- 用户能从激活页进入首页
- 首页能校验链接并创建任务
- 详情页能看到 `pending`、`running`、`completed`、`failed`

### 20.3 插件

首周目标：

- 完成 `installationId` 本地持久化
- 完成设备注册
- 完成 `license_token` 绑定
- 完成空队列轮询、任务认领、心跳
- 完成打开任务标签页和关闭标签页

首周交付标准：

- 插件能显示已安装、已绑定、空闲、执行中状态
- 后端能看到真实设备心跳
- 拿到任务后能成功打开目标商品页

## 21. 第二周派工建议

### 21.1 后端

- 完成上传签名和上传完成接口
- 完成结果入库接口
- 完成 ZIP 异步归档
- 完成转换任务表和转换接口

### 21.2 前端

- 完成结果图区渲染
- 完成失败提示和错误码映射
- 完成 ZIP 状态与下载区
- 完成历史任务页

### 21.3 插件

- 先完成淘宝主图 extractor
- 再完成拼多多主图 extractor
- 完成最小上传流程和结果回传
- 完成失败重试和错误码分类

## 22. 每日联调节奏建议

建议固定每日联调顺序：

1. 先检查卡密兑换和许可有效期
2. 再检查插件注册、绑定、心跳
3. 再检查任务创建和认领
4. 再检查单平台 extractor 结果
5. 最后检查详情页展示

建议每日联调结束前至少回答 4 个问题：

- 今天新增了哪些稳定接口
- 今天新增了哪些错误码
- 今天样本成功率是多少
- 明天联调会依赖谁先交付什么

## 23. 当前最小可开发范围

如果要尽快开工并控制复杂度，当前最小范围建议就是：

- 卡密时间授权
- 单设备绑定
- 单链接任务
- 淘宝主图提取
- 拼多多主图提取
- 任务详情页状态展示

以下内容都可以明确放到第二阶段：

- SKU 图完整分类
- 详情图完整分类
- ZIP 下载
- 图片格式转换
- 历史任务筛选
