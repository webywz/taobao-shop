# 前端产品与页面设计

## 1. 文档目标

本文档定义 Web 端页面结构、关键交互、页面状态和视觉方向。

## 2. 前端技术栈

- `Next.js`
- `TypeScript`
- `Tailwind CSS`
- `TanStack Query`
- `React Hook Form`

## 3. 信息架构

主导航建议：

- `提取`
- `历史`
- `帮助`

推荐路由：

- `/activate`
- `/extract`
- `/tasks`
- `/tasks/[taskId]`
- `/help`

## 4. 页面清单

首版页面：

- 卡密激活页
- 首页 / 任务创建页
- 历史任务页
- 任务详情页
- 帮助 / 插件安装引导页

全局访问规则：

- 未激活用户默认落到卡密激活页
- 已激活但插件未安装用户仍可进入首页，但主操作按钮禁用
- 历史页和详情页都要求存在有效 `license_token`

## 5. 卡密激活页

页面目标：

- 输入卡密
- 激活当前浏览器使用资格
- 绑定当前插件设备

主要模块：

- 卡密输入框
- 激活按钮
- 激活说明
- 常见失败提示
- 当前绑定状态提示

关键状态：

- 未激活
- 激活中
- 激活成功
- 激活失败

常见失败场景：

- 卡密无效
- 卡密已使用
- 卡密已过期
- 当前设备未完成绑定

补充交互：

- 输入框支持自动去空格和自动大写
- 卡密格式错误在提交前本地校验
- 激活成功后立即拉取当前绑定状态
- 若插件已安装但未绑定，页面给出“一键去插件完成绑定”的文案引导

激活成功后跳转首页。

## 6. 首页 / 任务创建页

页面目标：

- 输入商品链接
- 检查插件状态
- 创建任务

主要模块：

- 标题与说明
- URL 输入框
- 提取按钮
- 插件状态卡片
- 最近任务列表

关键状态：

- 插件未安装
- 插件已安装但未就绪
- 可创建任务
- 创建中
- 创建失败

交互建议：

- 未激活时跳转卡密激活页
- 创建成功后直接跳到详情页
- 插件未安装时明显提示去帮助页
- 链接校验失败时就地报错

输入校验建议：

- 支持粘贴后自动去掉追踪参数和空白字符
- 提交前识别平台并给出“淘宝商品”或“拼多多商品”标签
- 无法识别平台时，不允许提交
- 同一时刻只允许一个创建请求在前端进行中，避免重复提交

最近任务建议展示：

- 最近 5 到 10 条
- 状态徽标
- 失败原因摘要
- 快速跳转详情页入口

## 7. 历史任务页

目标：

- 查看历史结果
- 快速筛选任务状态

列表字段：

- 状态
- 平台
- 标题
- 来源链接
- 创建时间
- 完成时间
- 图片数量摘要

页面状态：

- 加载中
- 空列表
- 加载失败

筛选建议：

- 平台筛选
- 状态筛选
- 时间倒序
- 按标题或链接关键词搜索

列表操作建议：

- 点击行进入详情
- 对已完成任务直接展示 ZIP 下载快捷入口
- 对失败任务展示错误码 tooltip 或摘要

## 8. 任务详情页

目标：

- 展示单任务执行结果
- 提供归档和下载入口

主要模块：

- 任务基础信息
- 状态展示
- 错误码和失败原因
- 主图分组
- SKU 图分组
- 详情图分组
- 图片格式转换入口
- ZIP 状态与下载入口

关键状态：

- `pending`
- `claimed`
- `running`
- `uploading`
- `completed`
- `failed`
- `expired`

详情页交互建议：

- `pending` 到 `uploading` 阶段使用轮询刷新
- `completed` 后自动停止轮询
- `failed` 时优先展示可理解错误说明，再展示错误码
- 若 ZIP 未生成完成，下载区应显示“打包中”
- 图片加载失败时展示占位图和重试按钮

详情页信息块建议：

- 基础信息：平台、标题、来源链接、创建时间、完成时间
- 执行信息：当前状态、设备标识摘要、提取器版本、错误码
- 结果信息：主图数量、SKU 图数量、详情图数量、ZIP 状态
- 操作区：单图下载、分组下载、ZIP 下载、格式转换

## 9. 图片格式转换

详情页建议支持：

- 选择目标格式
- 单张图片转换
- 分组批量转换
- 转换中状态
- 转换完成后的下载入口

首版建议支持：

- `jpg`
- `png`
- `webp`

前端策略建议：

- 转换请求提交后立即返回任务状态，不阻塞详情页主线程
- 已转换产物与原图分开展示，但默认仍以原图为主
- 同一张图相同目标格式重复请求时可直接复用已有结果
- 批量转换要明确作用范围，例如“主图全部转 PNG”

## 10. 帮助页

内容建议：

- 为什么需要插件
- 插件安装步骤
- 插件绑定说明
- 卡密激活说明
- 卡密仅可兑换一次的说明
- 支持平台范围
- 常见错误说明
- 图片格式转换说明

帮助页模块建议：

- 首次使用流程图
- 插件安装检查清单
- 平台支持范围与已知限制
- 常见报错排查表
- 联系支持方式

## 11. 通用页面状态

### 11.1 加载态

- 列表用骨架屏
- 按钮用 loading
- 图片区用占位块

### 11.2 空态

- 历史页空态提示去创建任务
- 首页空态提示先激活卡密并安装插件

### 11.3 错误态

- 给出简洁标题
- 给出动作建议
- 尽量避免技术术语

建议统一错误呈现：

- 主标题描述结果，例如“商品页未能完成加载”
- 副文案说明下一步，例如“请确认当前浏览器可正常打开该商品链接后再试”
- 保留错误码便于客服或研发排查

## 12. UI 风格方向

视觉基调：

- 浅色
- 克制
- 工具感
- 偏专业

不建议：

- 过度科技蓝光效
- 大面积黑色控制台
- 花哨渐变
- 电商促销风

### 12.1 颜色

推荐方向：

- 主色：深青蓝或石墨蓝
- 辅助色：浅灰、米白、雾蓝
- 成功色：稳重绿色
- 警告色：琥珀色
- 错误色：深红

### 12.2 字体

中文：

- `PingFang SC`
- `Noto Sans SC`

英文和数字：

- `IBM Plex Sans`
- `Geist`

### 12.3 组件风格

- 卡片：轻边框、轻阴影、中等圆角
- 按钮：主按钮实心，次按钮描边
- 输入框：高度统一，聚焦边框清晰
- 状态标签：按任务状态区分颜色

细节建议：

- 首页输入区做成单核心动作区，减少装饰噪音
- 任务状态使用统一标签和进度提示，避免每页文案不一致
- 图片分组标题要带数量，帮助用户快速判断结果是否完整

## 13. 前端组件建议

- `LicenseActivationCard`
- `TaskStatusBadge`
- `PlatformBadge`
- `PluginStatusCard`
- `ImageGroupGrid`
- `ArchiveStatusCard`
- `ImageFormatConvertCard`
- `EmptyState`
- `ErrorState`
- `ActivationStatusBanner`
- `TaskCreateForm`
- `RecentTaskList`
- `SupportMatrixCard`
- `TaskErrorNotice`

## 14. 埋点建议

- 卡密激活点击
- 卡密激活成功
- 卡密激活失败
- 设备绑定成功
- 设备绑定失败
- 创建任务点击
- 插件安装引导点击
- 任务创建成功
- 任务创建失败
- ZIP 下载点击
- 图片格式转换提交
- 图片格式转换成功
- 图片格式转换失败

## 15. 前端状态管理建议

- `license` 信息建议放在全局 session store
- 任务列表和详情使用 `TanStack Query`
- 详情页轮询间隔建议 2 到 4 秒
- 激活态失效时统一清理缓存并跳转 `/activate`

## 16. 联调约定

- 所有错误提示优先依赖后端稳定错误码，而不是解析 message
- 创建任务接口支持 `Idempotency-Key`，前端提交时应携带
- 插件状态检测必须区分“未安装”和“已安装但未绑定”
- 详情页对 `archiveStatus` 和 `convertJobs` 采用独立查询，避免阻塞主结果渲染

## 17. 错误码前端呈现建议

推荐映射：

- `INVALID_ACTIVATION_CODE`
  - 标题：卡密无效
  - 说明：请检查输入内容是否完整
- `ACTIVATION_CODE_USED`
  - 标题：卡密已被使用
  - 说明：请更换卡密或联系提供方
- `INVALID_URL`
  - 标题：链接无法识别
  - 说明：请输入淘宝或拼多多商品详情页链接
- `UNSUPPORTED_PLATFORM`
  - 标题：暂不支持该平台
  - 说明：当前仅支持淘宝和拼多多
- `AUTH_REQUIRED`
  - 标题：需要登录后再提取
  - 说明：请先在浏览器中登录对应平台
- `PAGE_TIMEOUT`
  - 标题：页面加载超时
  - 说明：请确认商品页能正常打开后重试
- `PRODUCT_NOT_FOUND`
  - 标题：商品不存在或已失效
  - 说明：请检查链接是否可访问
- `UNSUPPORTED_LAYOUT`
  - 标题：当前商品页暂不支持
  - 说明：页面结构发生变化，建议记录样本排查
- `UPLOAD_FAILED`
  - 标题：图片上传失败
  - 说明：请稍后重试
- `ARCHIVE_FAILED`
  - 标题：ZIP 生成失败
  - 说明：可重新发起打包
- `CONVERT_FAILED`
  - 标题：格式转换失败
  - 说明：请重新提交转换任务

## 18. 轮询与刷新建议

详情页轮询建议：

- `pending`、`claimed`、`running`：每 2 秒轮询
- `uploading`：每 3 秒轮询
- `completed`、`failed`、`expired`：停止轮询

局部刷新建议：

- `archive` 状态单独轮询，不影响图片区
- `convertJobs` 单独轮询，不影响任务主状态
- 历史列表返回页面后可静默刷新一次

## 19. 页面按钮禁用规则

首页提取按钮禁用条件：

- 未激活
- 插件未安装
- 插件未绑定
- URL 为空
- URL 校验失败
- 当前有创建请求进行中

详情页下载按钮禁用条件：

- 任务未完成
- 对应资源下载链接未就绪
- ZIP 状态不是 `ready`

格式转换按钮禁用条件：

- 任务未完成
- 当前已有相同范围和格式的转换任务进行中
