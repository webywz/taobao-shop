# 产品范围与总体方案

## 1. 文档目标

本文档定义首版产品范围、核心边界、总体架构、角色分工和主链路。

## 2. 产品目标

目标体验：

1. 用户打开 Web
2. 粘贴淘宝或拼多多商品链接
3. 系统调起本地浏览器插件执行提取
4. 返回主图、SKU 图、详情图
5. 支持预览、历史查看和 ZIP 下载

## 3. 产品定位

这是一个“链接驱动的商品素材提取和归档工具”，不是云端全网抓图服务。

对外卖点：

- 输入链接即可提取商品图片
- 自动分类主图、SKU 图、详情图
- 自动归档并支持 ZIP 下载
- 支持历史留存和后续整理

## 4. 目标用户与角色

目标用户：

- 需要采集淘宝 / 拼多多商品图片的运营、设计、选品和投放人员
- 需要快速留档主图、SKU 图、详情图的中小团队
- 不具备开发能力，但愿意安装浏览器插件完成本地提取的业务用户

系统角色：

- `终端用户`：在 Web 中激活、创建任务、查看结果、下载素材
- `浏览器插件`：在本地浏览器上下文执行商品页访问和图片提取
- `后端服务`：负责激活、鉴权、任务状态、资产管理、归档与转换
- `运维 / 管理员`：查看日志、处理异常卡密、观察平台成功率

## 5. 首版范围

首版包含：

- 卡密激活
- 插件安装与设备绑定
- 单链接任务创建
- 淘宝 / 拼多多提取
- 图片上传和结果展示
- ZIP 归档
- 图片格式转换
- 历史任务

首版不包含：

- 支付平台
- 套餐购买
- 批量任务
- 多浏览器适配
- 团队权限
- 开放 API

## 6. 核心边界

- 不做云端集中抓取任意商品页
- 不设计代理池、账号池、验证码对抗能力
- 不承诺支持平台所有页面结构
- 不承诺绕过登录、权限和访问限制
- 用户必须在本地浏览器中具备访问商品页的能力
- 图片提取结果只覆盖当前 extractor 规则命中的可见素材

## 7. 关键术语

- `activation code`：一次性兑换卡密，只在首次激活使用
- `license`：兑换后的可用资格，驱动 Web 端所有业务访问
- `device`：已注册并可绑定 `license` 的插件实例
- `task`：单次商品链接提取任务
- `asset`：任务输出的单张图片对象
- `manifest`：插件回传的结构化提取结果
- `archive`：按任务聚合生成的 ZIP 包

## 8. 总体架构

```text
+-------------------+       HTTPS        +----------------------+
| Web SaaS          | <----------------> | API Service          |
| Next.js           |                    | NestJS / FastAPI     |
+-------------------+                    +----------------------+
         |                                           |
         | 状态查询 / 结果展示                        | DB / Queue / Storage
         v                                           v
+-------------------+                    +----------------------+
| Browser Extension |                    | Postgres / Redis     |
| MV3 + Plasmo      |                    | S3 / OSS             |
+-------------------+                    +----------------------+
         |
         | 用户本地浏览器上下文
         v
+-------------------+
| Taobao / PDD Page |
+-------------------+
```

职责边界：

- Web 负责用户输入、状态展示、结果消费和帮助引导
- 插件负责本地页面打开、平台识别、图片提取和文件直传
- API 负责卡密、任务、权限、结果持久化、签名 URL 和异步任务
- 队列 Worker 负责 ZIP 归档、格式转换、过期清理等后台处理

## 9. 核心流程

### 9.1 用户链路

1. 用户打开 Web
2. 用户输入卡密并完成激活
3. 用户确认插件已安装并绑定到当前激活许可
4. 输入商品链接并创建任务
5. 跳转详情页查看执行状态
6. 预览结果、下载图片、下载 ZIP、按需转换图片格式、查看历史

### 9.2 插件链路

1. 插件自动注册设备
2. 轮询拉取待执行任务
3. 认领任务
4. 打开商品页
5. 执行平台 extractor
6. 上传素材
7. 回传结果或错误

### 9.3 服务端链路

1. 创建任务
2. 调度设备认领
3. 接收进度、结果和失败
4. 写入结果和素材
5. 异步生成 ZIP
6. 返回下载链接

### 9.4 任务生命周期摘要

1. 用户创建任务后进入 `pending`
2. 空闲设备拉取并认领任务，进入 `claimed`
3. 插件开始执行，进入 `running`
4. 上传素材时进入 `uploading`
5. 所有结果入库后进入 `completed`
6. 任一关键步骤失败则进入 `failed`
7. 长时间无人认领或任务失效则进入 `expired`

## 10. 激活与访问

### 10.1 卡密激活

- 用户输入卡密后完成一次性激活
- 激活成功后生成 `license_token`
- 后续业务请求通过 `license_token` 标识当前使用资格

卡密模式建议：

- 一张卡密只允许兑换一次
- 兑换后绑定当前浏览器许可
- 卡密代表一段有效使用时间，不代表任务额度
- 插件需再绑定到当前许可后才能执行任务
- 用户后续使用时不再反复输入卡密

### 10.2 设备身份

- 每台插件设备有独立 `device token`
- 每个任务有短期 `task token`
- 设备只能执行当前激活许可关联的任务

### 10.3 图片访问

- 激活成功后，图片预览、原图获取和 ZIP 下载都不需要再单独输入卡密
- 图片和 ZIP 的访问权限跟随当前激活许可

### 10.4 图片格式转换

- 支持将提取结果转换为其他图片格式
- 首版建议支持 `jpg`、`png`、`webp`
- 转换可按单张图片或按分组批量触发
- ZIP 与转换产物保留期支持可配置，建议只开放 `3天`、`7天`、`30天`

## 11. 任务输入与输出约束

任务输入约束：

- 首版只接受单个商品链接
- 输入 URL 必须能识别为淘宝或拼多多商品详情页
- 非商品详情页、店铺页、活动页、搜索页直接拒绝
- 同一 `license` 下可存在多个历史任务，但首版建议限制并发创建频率

任务输出约束：

- 至少返回平台、来源链接、抓取时间、提取器版本
- 图片按 `main`、`sku`、`detail` 分组
- 所有图片应带上稳定 `assetId`，供预览、转换和下载复用
- 如果某一分组为空，应返回空数组而不是缺失字段

## 12. 提取结果模型

核心结果结构：

```json
{
  "platform": "taobao",
  "sourceUrl": "https://item.taobao.com/item.htm?id=123456",
  "canonicalUrl": "https://item.taobao.com/item.htm?id=123456",
  "productId": "123456",
  "title": "商品标题",
  "images": {
    "main": [],
    "sku": [],
    "detail": []
  },
  "meta": {
    "capturedAt": "2026-04-08T10:00:00Z",
    "extractorVersion": "1.0.0"
  }
}
```

建议每个 `asset` 额外保留：

- 原始 URL
- 上传后对象存储 key
- 图片宽高
- mime type
- 文件大小
- 归属分组
- 顺序序号
- 可选 `skuName`

## 13. 非功能要求

- 单链接任务正常情况下 60 秒内完成或失败
- API 支持无状态扩展
- ZIP 生成走异步队列
- 图片格式转换应异步或快速返回，避免阻塞结果页
- 卡密激活校验应稳定可追踪
- 卡密生成应具备足够随机性并全局唯一
- 关键流程具备日志和错误码
- 失败任务可追踪到平台、设备、错误码

## 14. 依赖与前置假设

- 用户使用桌面 Chrome，并能安装 MV3 插件
- 用户本地网络可正常访问淘宝 / 拼多多商品页
- 用户如遇登录态商品，需自行在浏览器中保持可用登录态
- 对象存储具备直传能力和签名下载能力
- 归档与转换有独立 Worker 进程处理异步任务

## 15. 成功标准

- 用户通过卡密激活后可稳定创建并完成单链接任务
- 淘宝和拼多多都能返回可用主图
- 结果页能清晰展示主图、SKU 图、详情图
- 成功任务能直接下载 ZIP
- 成功任务能直接获取图片资源
- 能稳定完成常见格式转换
- 常见失败场景能返回明确错误码

## 16. 明确不承诺项

- 不承诺提取视频、评论图片、买家秀图片
- 不承诺自动处理平台登录、滑块、风控校验
- 不承诺兼容所有第三方分销页或中间跳转页
- 不承诺生成面向外部客户的共享公开链接

## 17. 后续扩展

后续再考虑：

- 批量任务
- 团队协作
- 对象存储同步
- Webhook
- 支付与套餐购买
