{
  "name": "@tb-pdd-image/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn app.main:app --reload --port 8000",
    "install-deps": "pip install -r requirements.txt"
  }
}
